git --version / git -v

git status

git init (Only once per a File at the begining.)

git add .(to add all files to staging area) / git add <fileName> <fileName>

git commit -m "Message regarding the commit you just made."

git log

