git config --global user.name
git config --global user.name "Pranav Patil"

git config --global user.email
git config --global user.email "bosslevel616@gmail.com"

git config --global core.editor "code --wait" (sets default editor to VSCode)

- gitingonre
    . to create type "touch .gitignore" in the terminal